# Default ProGuard rules. Room and Compose ship their own consumer rules.

# Keep Room entities' no-arg state intact (Room generates code; nothing extra needed for KSP).
# ZXing core is pure Java with no reflection in the paths we use; default rules suffice.
